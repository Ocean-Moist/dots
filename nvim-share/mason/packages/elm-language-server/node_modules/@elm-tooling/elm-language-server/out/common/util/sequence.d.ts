export declare class Sequence<T> {
    private first;
    private getter;
    private mappers;
    constructor(first: T, getter: (value: T) => T | undefined);
    private iterator;
    map<U>(mapper: (value: T) => U): Sequence<U>;
    find(func: (value: T) => boolean): T | undefined;
    toArray(): T[];
}
