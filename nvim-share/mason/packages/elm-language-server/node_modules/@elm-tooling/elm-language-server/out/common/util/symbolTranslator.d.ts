import { SymbolInformation } from "vscode-languageserver";
import { SyntaxNode } from "web-tree-sitter";
export declare class SymbolInformationTranslator {
    static translateNodeToSymbolInformation(uri: string, node: SyntaxNode): SymbolInformation | undefined;
    private static createSymbolInformation;
}
