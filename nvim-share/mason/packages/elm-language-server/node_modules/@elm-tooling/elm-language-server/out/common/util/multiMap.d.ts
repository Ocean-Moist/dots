/**
 * A map that abtracts storing items with the same key
 */
export declare class MultiMap<K, V> extends Map<K, V | V[]> {
    get(key: K, filter?: (val: V) => boolean, sort?: (a: V, b: V) => number): V | undefined;
    getAll(key: K): V[] | undefined;
    set(key: K, val: V, equal?: (a: V, b: V) => boolean): this;
    replace(key: K, val: V): this;
    forEach(callbackfn: (value: V, key: K, map: Map<K, V | V[]>) => void): void;
}
