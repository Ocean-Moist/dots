import { ISourceFile } from "../../compiler/forest";
import { NodeType } from "./treeUtils";
import { SyntaxNode } from "web-tree-sitter";
import { IProgram } from "../../compiler/program";
export interface IPossibleImport {
    module: string;
    value: string;
    type: NodeType;
    node: SyntaxNode;
    valueToImport?: string;
    package?: string;
}
export declare class ImportUtils {
    static getPossibleImports(program: IProgram, sourceFile: ISourceFile): IPossibleImport[];
    static getPossibleImportsOfTree(tree: ISourceFile): IPossibleImport[];
    private static comparisonScore;
}
