import { CancellationToken } from "vscode-languageserver";
export declare class DiagnosticsRequest {
    readonly files: string[];
    static execute(requestor: (files: string[], delay: number, cancellationToken: CancellationToken) => Promise<void>, files: string[], onDone: () => void): DiagnosticsRequest;
    private done;
    private readonly token;
    private constructor();
    cancel(): void;
}
