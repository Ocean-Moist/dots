import { IDiagnostic } from "./diagnosticsProvider";
export declare const enum DiagnosticKind {
    ElmMake = 0,
    ElmTest = 1,
    ElmLS = 2,
    Syntactic = 3,
    Semantic = 4,
    Suggestion = 5,
    ElmReview = 6
}
export declare function diagnosticsEquals(a: IDiagnostic, b: IDiagnostic): boolean;
export declare class FileDiagnostics {
    uri: string;
    private diagnostics;
    constructor(uri: string);
    get(): IDiagnostic[];
    update(kind: DiagnosticKind, diagnostics: IDiagnostic[]): boolean;
    getForKind(kind: DiagnosticKind): IDiagnostic[];
}
