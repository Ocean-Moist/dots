import { SyntaxNode } from "web-tree-sitter";
export declare class SyntaxNodeMap<K extends SyntaxNode, V> {
    private map;
    set(key: K, value: V): void;
    get(key: K): V | undefined;
    getOrSet(key: K, setter: () => V): V;
    has(key: K): boolean;
    mapValues(callback: (value: V) => V): void;
    forEach(callback: (val: V, key: K) => void): void;
    clear(): void;
    delete(key: K): void;
}
