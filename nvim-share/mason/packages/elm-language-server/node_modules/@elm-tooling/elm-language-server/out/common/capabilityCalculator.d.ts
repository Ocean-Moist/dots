import { ClientCapabilities, ServerCapabilities } from "vscode-languageserver";
export declare class CapabilityCalculator {
    private clientCapabilities;
    constructor(clientCapabilities: ClientCapabilities);
    get capabilities(): ServerCapabilities;
}
