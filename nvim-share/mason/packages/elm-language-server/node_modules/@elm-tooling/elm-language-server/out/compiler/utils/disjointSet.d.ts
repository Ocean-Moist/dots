import { Type, TVar } from "../typeInference";
export declare class DisjointSet {
    private map;
    set(tvar: TVar, type: Type): void;
    get(type: Type): Type;
    contains(tvar: TVar): boolean;
    toMap(): Map<TVar, Type>;
}
